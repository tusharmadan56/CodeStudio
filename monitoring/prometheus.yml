global:
  scrape_interval: 15s

scrape_configs:
  # the backend runs on the host (systemd in prod, node locally), not in compose
  - job_name: codestudio-backend
    static_configs:
      - targets: ['host.docker.internal:3000']
